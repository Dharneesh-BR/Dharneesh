[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"

# Handle all SPA routes (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
